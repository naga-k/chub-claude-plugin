{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npm install -g @aisuite/chub 2>/dev/null && chub update 2>/dev/null"
          }
        ]
      }
    ]
  }
}
